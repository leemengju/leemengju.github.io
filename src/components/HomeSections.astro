---
/**
 * The full home-page structure carried over from the old Framer site:
 * Hero → About(+stats carousel) → Skills → Experience → Projects(+filter) →
 * Education → Contact. Shared by the zh (/) and en (/en/) home pages so the
 * two locales stay structurally identical; only content differs.
 */
import ProjectCard from './ProjectCard.astro';
import StatsBand from './StatsBand.tsx';
import FloatingNav from './FloatingNav.astro';
import RainingLetters from './effects/RainingLetters.tsx';
import LetterSwapTitle from './effects/LetterSwapTitle.tsx';
import SmokyText from './effects/SmokyText.tsx';
import { strings, categoryLabels, type Locale } from '../lib/i18n';
import { getProfile } from '../lib/profile';
import type { ProjectEntry } from '../lib/projects';

interface Props {
  locale: Locale;
  projects: ProjectEntry[];
}
const { locale, projects } = Astro.props as Props;
const t = strings[locale];
const p = getProfile(locale);

const navItems = [
  { id: 'about', label: t.aboutHeading },
  { id: 'skills', label: t.skillsHeading },
  { id: 'experience', label: t.experienceHeading },
  { id: 'projects', label: t.projectsHeading },
  { id: 'education', label: t.educationHeading },
  { id: 'contact', label: t.contactHeading }
];

// Filter pills: only categories actually used by at least one project.
const usedCategories = Object.keys(categoryLabels).filter((key) =>
  projects.some((project) => (project.data.categories ?? []).includes(key))
);

// Inline stroke icons for skill cards (24×24, stroke = currentColor). Hover
// styling (accent stroke + soft fill) lives in CSS below.
const skillIcons: Record<string, string> = {
  refactor:
    '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="7" height="7" rx="1.5"/><rect x="14" y="3" width="7" height="7" rx="1.5"/><rect x="14" y="14" width="7" height="7" rx="1.5"/><path d="M6.5 10v4a2 2 0 0 0 2 2H14"/></svg>',
  database:
    '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><ellipse cx="12" cy="5" rx="8" ry="3"/><path d="M4 5v14c0 1.66 3.58 3 8 3s8-1.34 8-3V5"/><path d="M4 12c0 1.66 3.58 3 8 3s8-1.34 8-3"/></svg>',
  chart:
    '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><path d="M3 3v18h18"/><rect x="7" y="12" width="3" height="6" rx="0.8"/><rect x="12" y="8" width="3" height="10" rx="0.8"/><rect x="17" y="5" width="3" height="13" rx="0.8"/></svg>',
  plug:
    '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><path d="M9 7V3M15 7V3"/><path d="M7 7h10v4a5 5 0 0 1-5 5 5 5 0 0 1-5-5V7Z"/><path d="M12 16v5"/></svg>',
  stack:
    '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><path d="m12 3 9 5-9 5-9-5 9-5Z"/><path d="m3 13 9 5 9-5"/><path d="m3 17.5 9 5 9-5"/></svg>',
  kanban:
    '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M8 7v10M12.5 7v6M17 7v8"/></svg>'
};

const githubIcon =
  '<svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M12 .5C5.65.5.5 5.65.5 12c0 5.08 3.29 9.39 7.86 10.91.58.11.79-.25.79-.55 0-.27-.01-1.17-.02-2.12-3.2.7-3.88-1.36-3.88-1.36-.52-1.33-1.28-1.68-1.28-1.68-1.04-.71.08-.7.08-.7 1.15.08 1.76 1.19 1.76 1.19 1.03 1.75 2.69 1.25 3.34.95.1-.74.4-1.25.72-1.54-2.55-.29-5.24-1.28-5.24-5.68 0-1.26.45-2.28 1.19-3.09-.12-.29-.52-1.46.11-3.05 0 0 .97-.31 3.18 1.18a11.1 11.1 0 0 1 5.8 0c2.2-1.49 3.17-1.18 3.17-1.18.63 1.59.23 2.76.12 3.05.74.81 1.18 1.83 1.18 3.09 0 4.41-2.69 5.38-5.25 5.66.41.35.77 1.05.77 2.12 0 1.53-.01 2.76-.01 3.14 0 .3.2.67.8.55A11.52 11.52 0 0 0 23.5 12C23.5 5.65 18.35.5 12 .5Z"/></svg>';
const linkedinIcon =
  '<svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M20.45 20.45h-3.55v-5.57c0-1.33-.03-3.04-1.85-3.04-1.86 0-2.14 1.45-2.14 2.94v5.67H9.36V9h3.41v1.56h.05c.47-.9 1.63-1.85 3.36-1.85 3.6 0 4.27 2.37 4.27 5.45v6.29ZM5.34 7.43a2.06 2.06 0 1 1 0-4.12 2.06 2.06 0 0 1 0 4.12ZM7.12 20.45H3.55V9h3.57v11.45Z"/></svg>';
---

<FloatingNav items={navItems} />

<section class="hero">
  <RainingLetters client:visible text={t.introText} />
  <div class="hero__cta">
    {
      p.contact.resume && (
        <a class="btn btn--primary" href={p.contact.resume} target="_blank" rel="noopener noreferrer">
          {t.resumeButton}
        </a>
      )
    }
    <a class="btn btn--icon" href={p.contact.github} target="_blank" rel="noopener noreferrer" aria-label="GitHub">
      <Fragment set:html={githubIcon} />
    </a>
    {
      p.contact.linkedin && (
        <a class="btn btn--icon" href={p.contact.linkedin} target="_blank" rel="noopener noreferrer" aria-label="LinkedIn">
          <Fragment set:html={linkedinIcon} />
        </a>
      )
    }
  </div>
</section>

<section id="about" class="section">
  <h2><LetterSwapTitle client:visible text={t.aboutHeading} /></h2>
  {p.about.map((para) => <p class="about__para" data-reveal>{para}</p>)}
  <StatsBand stats={p.stats} client:visible />
</section>

<section id="skills" class="section">
  <h2><LetterSwapTitle client:visible text={t.skillsHeading} /></h2>
  <ul class="skills">
    {
      p.skills.map((s) => (
        <li class="skill" data-reveal>
          <span class="skill__icon" aria-hidden="true">
            <Fragment set:html={skillIcons[s.icon]} />
          </span>
          <span class="skill__name">{s.name}</span>
          <span class="skill__desc">{s.desc}</span>
        </li>
      ))
    }
  </ul>
</section>

<section id="experience" class="section">
  <h2><LetterSwapTitle client:visible text={t.experienceHeading} /></h2>
  <ol class="exp">
    {
      p.experience.map((e) => (
        <li class="exp__item" data-reveal>
          <div class="exp__head">
            <span class="exp__role">{e.role}</span>
            <span class="exp__period">{e.period}</span>
          </div>
          <div class="exp__org">{e.org}</div>
          {e.summary && <p class="exp__summary">{e.summary}</p>}
          <ul class="exp__highlights">
            {e.highlights.map((h) => <li>{h}</li>)}
          </ul>
        </li>
      ))
    }
  </ol>
</section>

<section id="projects" class="section">
  <h2><LetterSwapTitle client:visible text={t.projectsHeading} /></h2>
  <div class="filter" role="group">
    <button class="filter__pill is-active" data-filter="all" type="button">{t.filterAll}</button>
    {
      usedCategories.map((key) => (
        <button class="filter__pill" data-filter={key} type="button">
          {categoryLabels[key][locale]}
        </button>
      ))
    }
  </div>
  <ul class="project-list" data-page-size="6">
    {projects.map((project) => <ProjectCard project={project} locale={locale} />)}
  </ul>
  <nav class="projects-pager" aria-label={locale === 'en' ? 'Pagination' : '分頁'} hidden></nav>
</section>

<section id="education" class="section">
  <h2><LetterSwapTitle client:visible text={t.educationHeading} /></h2>
  <ol class="edu">
    {
      p.education.map((e) => (
        <li class="edu__item" data-reveal>
          <div class="exp__head">
            <span class="exp__role">
              {e.school} — {e.degree}
            </span>
            <span class="exp__period">{e.period}</span>
          </div>
          <ul class="exp__highlights">
            {e.notes.map((n) => <li>{n}</li>)}
          </ul>
        </li>
      ))
    }
  </ol>
</section>

<section id="contact" class="section">
  <h2><LetterSwapTitle client:visible text={t.contactHeading} /></h2>
  <p class="contact-cta">
    <span class="contact-cta__wave" aria-hidden="true">👋</span>
    {locale === 'en'
      ? "Let's build great experiences together — reach me anytime:"
      : '一起打造好體驗——隨時歡迎透過以下方式找我:'}
  </p>
  <ul class="contact">
    <li><span class="contact__k">{t.contactEmail}</span><a href={`mailto:${p.contact.email}`}><SmokyText client:visible text={p.contact.email} colorVar="var(--text)" intensity={11} /></a></li>
    <li><span class="contact__k">{t.contactGitHub}</span><a href={p.contact.github} target="_blank" rel="noopener noreferrer"><SmokyText client:visible text={p.contact.github.replace('https://', '')} colorVar="var(--text)" intensity={11} /></a></li>
    <li><span class="contact__k">{t.contactLocation}</span><SmokyText client:visible text={p.contact.location} colorVar="var(--text)" intensity={11} /></li>
  </ul>
</section>

<script>
  // Projects: category filter + list pagination (6/page). List is the only
  // view mode; changing the filter resets to page 1.
  const section = document.getElementById('projects');
  if (section) {
    const pills = [...section.querySelectorAll<HTMLButtonElement>('.filter__pill')];
    const list = section.querySelector<HTMLElement>('.project-list')!;
    const cards = [...section.querySelectorAll<HTMLElement>('.project-card[data-categories]')];
    const pager = section.querySelector<HTMLElement>('.projects-pager')!;
    const PAGE = parseInt(list.dataset.pageSize || '6', 10);

    let category = 'all';
    let page = 0;

    const filtered = () =>
      cards.filter((c) => category === 'all' || (c.dataset.categories || '').split(' ').includes(category));

    const buildPager = (pages: number) => {
      pager.innerHTML = '';
      pager.hidden = pages <= 1;
      if (pages <= 1) return;
      const mk = (label: string, target: number, opts: { active?: boolean; disabled?: boolean } = {}) => {
        const b = document.createElement('button');
        b.type = 'button';
        b.textContent = label;
        if (opts.active) b.classList.add('is-active');
        if (opts.disabled) b.disabled = true;
        b.addEventListener('click', () => {
          page = target;
          render();
          list.scrollIntoView({ behavior: 'smooth', block: 'start' });
        });
        pager.appendChild(b);
      };
      mk('‹', Math.max(0, page - 1), { disabled: page === 0 });
      for (let i = 0; i < pages; i++) mk(String(i + 1), i, { active: i === page });
      mk('›', Math.min(pages - 1, page + 1), { disabled: page === pages - 1 });
    };

    function render() {
      const set = filtered();
      const pages = Math.max(1, Math.ceil(set.length / PAGE));
      if (page >= pages) page = pages - 1;
      const visible = new Set(set.slice(page * PAGE, page * PAGE + PAGE));
      cards.forEach((c) => {
        const on = visible.has(c);
        c.hidden = !on;
        if (on) c.classList.add('is-visible'); // paged-in cards never stuck at opacity:0
      });
      buildPager(pages);
    }

    pills.forEach((pill) =>
      pill.addEventListener('click', () => {
        category = pill.dataset.filter || 'all';
        page = 0;
        pills.forEach((b) => b.classList.toggle('is-active', b === pill));
        render();
      })
    );

    render();
  }
</script>

<script>
  // Timeline beam (item 15): for the Experience + Education lists, grow an
  // accent line down the rail as the section scrolls through the viewport.
  const lists = [...document.querySelectorAll<HTMLElement>('.exp, .edu')];
  const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  if (lists.length && !reduced) {
    const beams = lists.map((list) => {
      const beam = document.createElement('span');
      beam.className = 'tl-beam';
      beam.setAttribute('aria-hidden', 'true');
      list.appendChild(beam);
      return { list, beam };
    });
    let ticking = false;
    const update = () => {
      ticking = false;
      const vh = window.innerHeight || document.documentElement.clientHeight;
      for (const { list, beam } of beams) {
        const r = list.getBoundingClientRect();
        // 0 when the list top sits at 60% down the viewport, 1 once its bottom passes
        const p = Math.max(0, Math.min(1, (vh * 0.6 - r.top) / Math.max(1, r.height)));
        beam.style.height = (p * r.height).toFixed(1) + 'px';
      }
    };
    const onScroll = () => {
      if (!ticking) {
        ticking = true;
        requestAnimationFrame(update);
      }
    };
    window.addEventListener('scroll', onScroll, { passive: true });
    window.addEventListener('resize', onScroll);
    update();
  }
</script>

<style>
  .hero {
    position: relative;
    min-height: min(70vh, 600px);
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 4rem 0 3.5rem;
    margin-bottom: 0.5rem;
    overflow: hidden;
  }
  /* keep hero content above the raining-letters layer */
  .hero > .rain-title,
  .hero > .hero__cta {
    position: relative;
    z-index: 1;
  }
  /* raining-letters background (item 5) */
  .rain-layer {
    position: absolute;
    inset: 0;
    z-index: 0;
    overflow: hidden;
    pointer-events: none;
    /* dissolve into the next section instead of a hard edge */
    -webkit-mask-image: linear-gradient(to bottom, #000 42%, transparent 96%);
    mask-image: linear-gradient(to bottom, #000 42%, transparent 96%);
  }
  .rain-layer :global(.rain__glyph) {
    position: absolute;
    transform: translate(-50%, -50%);
    font-family: ui-monospace, 'SFMono-Regular', Menlo, monospace;
    font-size: 1.1rem;
    color: var(--text);
    opacity: 0.12;
    will-change: top;
    user-select: none;
  }
  .rain-layer :global(.rain__glyph.is-lit) {
    color: var(--accent);
    opacity: 0.9;
    text-shadow: 0 0 8px color-mix(in srgb, var(--accent) 55%, transparent);
  }
  .rain-title {
    margin: 0 0 0.5rem;
    max-width: 24ch;
    font-size: clamp(1.9rem, 4.6vw, 3.2rem);
    font-weight: 800;
    letter-spacing: -0.02em;
    line-height: 1.3;
  }
  .rain-title :global(.rain-title__dud) {
    color: var(--accent);
    opacity: 0.85;
  }
  .hero__cta {
    display: flex;
    align-items: center;
    gap: 0.7rem;
    margin-top: 1.75rem;
  }
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    text-decoration: none;
    color: inherit;
    border: 1px solid var(--border-strong);
    border-radius: 999px;
    font-weight: 600;
    transition: transform 0.2s ease, background 0.2s ease, color 0.2s ease, border-color 0.2s ease;
  }
  .btn:hover {
    transform: translateY(-2px);
  }
  .btn--primary {
    padding: 0.6rem 1.4rem;
    background: var(--accent);
    border-color: var(--accent);
    color: #fff;
    font-size: 0.95rem;
  }
  .btn--primary:hover {
    filter: brightness(1.08);
  }
  .btn--icon {
    width: 2.6rem;
    height: 2.6rem;
    padding: 0;
  }
  .btn--icon :global(svg) {
    width: 1.25rem;
    height: 1.25rem;
  }
  .btn--icon:hover {
    color: var(--accent);
    border-color: var(--accent);
    background: var(--accent-soft);
  }

  .section {
    padding: 3.5rem 0 0.5rem;
    scroll-margin-top: 4.5rem;
  }
  .section > h2 {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    font-size: 1.25rem;
    font-weight: 700;
    margin: 0 0 1.75rem;
    color: var(--accent);
  }
  .section > h2::before {
    content: '';
    display: inline-block;
    width: 0.5rem;
    height: 0.5rem;
    border-radius: 50%;
    background: var(--accent);
  }
  .about__para {
    margin: 0 0 1rem;
    max-width: 46rem;
    font-size: 1.02rem;
    line-height: 1.75;
  }

  .skills {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 0.85rem;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
  }
  .skill {
    padding: 1.25rem 1.35rem;
    background: var(--surface);
    border: 1px solid var(--border-soft);
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
  }
  .skill:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-hover);
    border-color: var(--accent);
  }
  .skill__icon {
    display: inline-flex;
    width: 2.6rem;
    height: 2.6rem;
    align-items: center;
    justify-content: center;
    border-radius: 0.7rem;
    background: var(--fill-softer);
    border: 1px solid var(--border-soft);
    margin-bottom: 0.8rem;
    color: var(--text-muted);
    transition: color 0.25s ease, background 0.25s ease, border-color 0.25s ease, transform 0.25s ease;
  }
  .skill__icon :global(svg) {
    width: 1.4rem;
    height: 1.4rem;
  }
  /* Hover: outline icon "fills in" with the accent treatment. */
  .skill:hover .skill__icon {
    color: var(--accent);
    background: var(--accent-soft);
    border-color: var(--accent);
    transform: scale(1.08) rotate(-3deg);
  }
  @media (prefers-reduced-motion: reduce) {
    .skill,
    .skill__icon {
      transition: none;
    }
    .skill:hover {
      transform: none;
    }
    .skill:hover .skill__icon {
      transform: none;
    }
  }
  .skill__name {
    display: block;
    font-weight: 700;
    margin-bottom: 0.4rem;
  }
  .skill__desc {
    font-size: 0.9rem;
    color: var(--text-muted);
    line-height: 1.65;
  }

  .exp,
  .edu {
    list-style: none;
    margin: 0;
    padding: 0 0 0 1.25rem;
    display: grid;
    gap: 1.75rem;
    border-left: 2px solid var(--border-soft);
    position: relative;
  }
  /* timeline beam (item 15): an accent line over the faint rail that fills as
     the section scrolls into view */
  .tl-beam {
    position: absolute;
    left: -2px;
    top: 0;
    width: 2px;
    height: 0;
    border-radius: 2px;
    background: linear-gradient(
      to bottom,
      var(--accent),
      color-mix(in srgb, var(--accent) 25%, transparent)
    );
    will-change: height;
  }
  @media (prefers-reduced-motion: reduce) {
    .tl-beam {
      display: none;
    }
  }
  .exp__item,
  .edu__item {
    position: relative;
  }
  .exp__item::before,
  .edu__item::before {
    content: '';
    position: absolute;
    left: -1.56rem;
    top: 0.3rem;
    width: 0.6rem;
    height: 0.6rem;
    border-radius: 50%;
    background: var(--accent);
  }
  .exp__head {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: 1rem;
    flex-wrap: wrap;
  }
  .exp__role {
    font-weight: 700;
  }
  .exp__period {
    font-size: 0.85rem;
    opacity: 0.65;
    font-variant-numeric: tabular-nums;
    white-space: nowrap;
  }
  .exp__org {
    opacity: 0.8;
    margin-top: 0.15rem;
  }
  .exp__summary {
    margin: 0.5rem 0 0.25rem;
    opacity: 0.85;
    font-size: 0.92rem;
  }
  /* Custom accent bullets: `display:grid` on a UL suppresses native ::marker
     rendering in some engines — the 1440px "no bullets" bug — so draw our own. */
  .exp__highlights {
    list-style: none;
    margin: 0.5rem 0 0;
    padding-left: 0;
    display: grid;
    gap: 0.3rem;
  }
  .exp__highlights li {
    font-size: 0.92rem;
    position: relative;
    padding-left: 1.1rem;
  }
  .exp__highlights li::before {
    content: '';
    position: absolute;
    left: 0.15rem;
    top: 0.55em;
    width: 0.35rem;
    height: 0.35rem;
    border-radius: 50%;
    background: var(--accent);
    opacity: 0.75;
  }

  /* Segmented control: one inset track holding all pills, with a single
     sliding accent "thumb" behind the active one — reads as a considered
     control rather than a row of loose chips. */
  .filter {
    display: inline-flex;
    flex-wrap: wrap;
    gap: 0.15rem;
    margin-bottom: 1.75rem;
    padding: 0.3rem;
    background: var(--fill-softer);
    border: 1px solid var(--border-soft);
    border-radius: 999px;
  }
  .filter__pill {
    font: inherit;
    font-size: 0.82rem;
    font-weight: 600;
    color: var(--text-muted);
    background: transparent;
    border: none;
    border-radius: 999px;
    padding: 0.4rem 0.95rem;
    cursor: pointer;
    transition: color 0.25s ease, background 0.25s ease;
  }
  .filter__pill:hover {
    color: var(--text);
    background: var(--fill-soft);
  }
  .filter__pill.is-active {
    color: #fff;
    background: var(--accent);
    box-shadow: 0 2px 8px -2px color-mix(in srgb, var(--accent) 55%, transparent);
  }
  .filter__pill:focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 2px;
  }

  /* Mobile (item 9): the pill-shaped segmented track wraps into an ugly
     multi-row blob, so fall back to loose bordered chips. Active/press use the
     secondary grey fill rather than the accent. */
  @media (max-width: 700px) {
    .filter {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
      padding: 0;
      background: none;
      border: none;
      border-radius: 0;
    }
    .filter__pill {
      border: 1px solid var(--border-soft);
    }
    .filter__pill.is-active {
      color: var(--text);
      background: var(--fill-soft);
      border-color: var(--border-strong);
      box-shadow: none;
    }
    .filter__pill:active {
      background: var(--fill-soft);
    }
  }

  /* Projects are a single-column list of compact rows (item 9: list only). */
  .project-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.85rem;
  }
  /* row layout; reserve 3rem on the right so nothing crowds the arrow icon.
     `ul.` raises specificity so this beats ProjectCard's own scoped `a` rule. */
  ul.project-list :global(.project-card a) {
    flex-direction: row;
    flex-wrap: wrap;
    align-items: baseline;
    gap: 0.3rem 1rem;
    padding: 1.05rem 3rem 1.05rem 1.3rem;
  }
  .project-list :global(.project-card__title) {
    font-size: 1.05rem;
    padding-right: 0;
    flex: 1 1 58%;
  }
  .project-list :global(.project-card__meta) {
    margin-top: 0;
    text-align: right;
    flex: 1 1 auto;
  }
  .project-list :global(.project-card__metrics) {
    margin-top: 0.15rem;
    flex-basis: 100%;
  }
  .project-list :global(.project-card .tag-row) {
    margin-top: 0.1rem;
    padding-top: 0.35rem;
    flex-basis: 100%;
  }
  /* keep the arrow clear of the date (item 9: ≥6px — here the 3rem right
     padding above leaves a wide, comfortable gap) */
  .project-list :global(.project-card__arrow) {
    top: 1.15rem;
    right: 1.3rem;
  }

  /* pagination — border/fill use the secondary token (item 13) */
  .projects-pager {
    display: flex;
    flex-wrap: wrap;
    gap: 0.3rem;
    justify-content: center;
    margin-top: 1.75rem;
  }
  .projects-pager[hidden] {
    display: none;
  }
  .projects-pager button {
    font: inherit;
    font-size: 0.85rem;
    font-weight: 600;
    min-width: 2rem;
    height: 2rem;
    padding: 0 0.5rem;
    color: var(--text-muted);
    background: transparent;
    border: 1px solid var(--secondary);
    border-radius: 0.5rem;
    cursor: pointer;
    transition: color 0.2s ease, background 0.2s ease, border-color 0.2s ease;
  }
  .projects-pager button:hover:not(:disabled) {
    color: var(--text);
    background: color-mix(in srgb, var(--secondary) 18%, transparent);
  }
  .projects-pager button.is-active {
    color: #fff;
    background: var(--secondary);
    border-color: var(--secondary);
  }
  .projects-pager button:disabled {
    opacity: 0.4;
    cursor: default;
  }

  /* Contact CTA (item 17): a friendly animated wave to lift the section. */
  .contact-cta {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    margin: 0 0 1.5rem;
    font-size: 1.1rem;
    font-weight: 600;
  }
  .contact-cta__wave {
    display: inline-block;
    font-size: 1.5rem;
    transform-origin: 70% 70%;
    animation: cta-wave 2.4s ease-in-out infinite;
  }
  @keyframes cta-wave {
    0%, 60%, 100% { transform: rotate(0deg); }
    10% { transform: rotate(14deg); }
    20% { transform: rotate(-8deg); }
    30% { transform: rotate(14deg); }
    40% { transform: rotate(-4deg); }
    50% { transform: rotate(10deg); }
  }
  @media (prefers-reduced-motion: reduce) {
    .contact-cta__wave {
      animation: none;
    }
  }
  .contact {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 0.6rem;
  }
  .contact li {
    display: flex;
    gap: 0.75rem;
    align-items: baseline;
  }
  .contact__k {
    min-width: 5rem;
    opacity: 0.6;
    font-size: 0.85rem;
  }
  .contact a {
    color: inherit;
  }
</style>
